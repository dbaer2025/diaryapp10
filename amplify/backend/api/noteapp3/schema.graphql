type Diary @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  address: String
  website: String
}

type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  image: String
  author: String
} 